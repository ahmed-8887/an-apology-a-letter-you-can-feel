{
  "name": "an-apology-a-letter-you-can-feel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2",
    "nodemailer": "^9.0.6"
  },
  "engines": {
    "node": "20"
  }
}
